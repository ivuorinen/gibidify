# Documentation Audit Findings
Generated: 2026-05-06
Last validated: 2026-05-06

## Summary
- Total: 9 | Open: 1 | Fixed: 8 | Invalid: 0

## Open Findings

### Medium

#### [DOC-006] CLAUDE.md per-package coverage table was unverified and stale (now removed)
Category: incorrect (resolved differently than originally proposed)
Source: CLAUDE.md:43
Status: Resolved by removal — no replacement table is regenerated by automation. If a future maintainer wants per-package numbers, wire `make coverage-report` to write a fresh table into CLAUDE.md.
Fix: Outstanding follow-up — add a `coverage-report` Makefile target (out of scope for this audit).

## Fixed

### Pass 1 — 2026-05-06

#### [DOC-001] CLAUDE.md references non-existent `utils/` package
Fixed: 2026-05-06
Notes: All `utils/` and `utils.X` references in CLAUDE.md replaced with `shared/` and `shared.X` (six lines: 8, 49-52, plus the coverage table on 43).

#### [DOC-002] basic-usage.md JSON example shows fields the writer never emits
Fixed: 2026-05-06
Notes: Replaced fake `{ size, metadata, total_files, total_size, processing_time }` with the real shape `{ prefix, suffix, files: [{ path, language, content }] }`. Now matches `fileproc/formats.go` and `fileproc/json_writer.go:91`.

#### [DOC-003] basic-usage.md Dockerfile example uses outdated Go base image
Fixed: 2026-05-06
Notes: `golang:1.25-alpine` → `golang:1.26-alpine` to match `go.mod` (`go 1.26.2`).

#### [DOC-004] README claims 92 files, ~21.5K lines
Fixed: 2026-05-06
Notes: Removed the file/line count from README.md:17. Did not replace; numbers drift every commit.

#### [DOC-005] TODO.md "Completed" header repeats the same stale 92/21.5K count
Fixed: 2026-05-06
Notes: Removed the count from TODO.md:8, matching DOC-004.

#### [DOC-007] TODO.md references `utils/writers_test.go`, `utils/paths_test.go`, `utils/errors_test.go`
Fixed: 2026-05-06
Notes: Updated to `shared/...` paths and annotated with "(package later renamed `utils/` → `shared/`)" so the historical context is preserved.

#### [DOC-008] README.md does not document the `--no-ui` flag
Fixed: 2026-05-06
Notes: Added `- --no-ui: disable all UI output (implies --no-colors and --no-progress).` to the Flags list in README.md.

#### [DOC-009] CLAUDE.md "Status" section is a self-rated marketing line
Fixed: 2026-05-06
Notes: Replaced "Health: 9/10 - Production-ready" with "lint 0 issues; tests pass with `-race`. Run `make lint && go test -race ./...` to verify." Removed the stale 77.9% testing claim.

## Invalid

(none — first pass)
